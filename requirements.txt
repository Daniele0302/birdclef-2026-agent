# requirements.txt — Dependencies for the BirdCLEF 2026 agent
#
# Install with:   pip install -r requirements.txt
#
# If you have trouble with tensorflow, try:
#   pip install tensorflow==2.15.0
# Or on Mac M1/M2/M3:
#   pip install tensorflow-macos tensorflow-metal

# === Deep Learning framework ===
tensorflow>=2.13.0        # Neural network (CNN, training, etc.)

# === Audio processing ===
librosa>=0.10.0           # Audio loading + mel-spectrogram
soundfile>=0.12.0         # Backend for reading .ogg files

# === Data science ===
numpy>=1.24.0             # Numeric array operations
pandas>=2.0.0             # Tabular (CSV) manipulation
scikit-learn>=1.3.0       # Train/test split, metrics

# === Visualisation (optional, for notebooks) ===
matplotlib>=3.7.0         # Plots and charts

# === LLM communication ===
requests>=2.31.0          # HTTP client for the Ollama API
ollama>=0.1.0             # Official Ollama Python client (alternative to requests)

# === Utilities ===
tqdm>=4.65.0              # Progress bars
